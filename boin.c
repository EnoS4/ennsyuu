/*入力された英字の中から母音を出力するプログラムを作成せよ*/
/*動かない*/
/*#include<stdio.h>
#include<string.h>

#define max 256

int main(void) {
  int cnt;
  char imp[max];
  printf("英字を入力してください\n", imp[cnt]);
    scanf("%s\n", imp[cnt]);//
  for (cnt = 0; cnt < strlen(imp); cnt++) {
    switch (imp[cnt]){
      case'a':
      printf("%s\n",imp[cnt]);
      break;
      case'i':
      printf("%s\n",imp[cnt]);
      break;
      case'u':
      printf("%s\n",imp[cnt]);
      break;
      case'e':
      printf("%s\n",imp[cnt]);
      break;
      case'o':
      printf("%s\n",imp[cnt]);
      break;
    }
    printf("\n");
  }
  return 0;
}
*/
