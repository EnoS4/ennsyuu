/*入力された英字の中から母音を出力するプログラムを作成せよ*/
/*動かない*/
/*#include<stdio.h>
#include<string.h>

#define max 256

int main(void) {
  int cnt;
  cnt = 0;
  char a = 'a';
  char i = 'i';
  char u = 'u';
  char e = 'e';
  char o = 'o';
  char imp[max];
  printf("英字を入力してください\n");
    scanf("%c\n", imp[cnt]);
  for (cnt = 0; cnt < strlen(imp); cnt++) {
    switch (imp[cnt]){
      case 'a':
      case 'i':
      case 'u':
      case 'e':
      case 'o':
      printf("%c\n",imp[cnt]);
      break;
      default:
      break;
    }
    printf("\n");
  }
  return 0;
}
*/
